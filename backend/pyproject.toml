[project]
name = "ada"
version = "0.1.0"
description = "Ada — an autonomous career agent. Day-1 slice: paywalled ATS CV rewrite."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "pgvector>=0.3",
    "google-genai>=1.0",
    "langgraph>=0.2",
    "stripe>=10",
    "httpx>=0.27",
    "structlog>=24.1",
    "email-validator>=2.1",
    "alembic>=1.13",
    "bcrypt>=4.1",
    "google-cloud-storage>=2.16",
    "pypdf>=4.2",
    "python-docx>=1.1",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5",
    "pytest-bdd>=7",
    "ruff>=0.5",
    "mypy>=1.10",
    "mutmut>=3.6",
    "bandit>=1.7",
    "import-linter>=2",
    "aiosqlite>=0.20",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ada"]

[tool.ruff]
line-length = 100
target-version = "py311"
[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# B008: `Depends(...)` in defaults is the canonical FastAPI dependency pattern.
ignore = ["B008"]

[tool.mutmut]
source_paths = [
    "src/ada/ingest/normalize.py",
    "src/ada/auth/tokens.py",
    "src/ada/auth/passwords.py",
    "src/ada/payments/paystack.py",
]
also_copy = ["src/ada/", "tests/", "pyproject.toml"]
pytest_add_cli_args_test_selection = [
    "tests/test_ingest.py",
    "tests/test_auth.py",
    "tests/test_paystack.py",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
# One event loop for the whole test session: the app's engine pools asyncpg
# connections, and per-test loops make a pooled connection created in one test
# blow up ("attached to a different loop") when the next test checks it out.
asyncio_default_test_loop_scope = "session"
asyncio_default_fixture_loop_scope = "session"
testpaths = ["tests"]
